class Solution {
    public int reverse(int x) {
        int rev=0;
        int num=x;
        while(num!=0)
        {
            int digit=num%10;
            num=num/10;
            rev=rev*10+digit;
        }
        return rev;
    }
}
