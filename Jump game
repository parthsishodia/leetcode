class Solution {
    public boolean canJump(int[] nums) {
        int maxx=0;
        for(int i=0;i<=maxx;i++)
        {
            maxx=Math.max(maxx,i+array[i]);
            if(maxx>=array.lenght-1)
            {
                return true;
            }
        }
        return false;
    }
}
